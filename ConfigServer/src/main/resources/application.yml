server:
  port: 9999

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/thisischetanyadav/microservices_config
          default-label: main
          clone-on-start: true
          search-paths: configs